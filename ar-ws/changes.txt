# PHASE 6: ESTRUTURAÇÃO E HARDENING FINAL

## 👨‍🍳 A RECEITA (O QUE E COMO FAZER)

Para atingir a arquitetura de 3 pilares, seguiremos estes passos técnicos:

1.  **LIMPEZA DO AR-BEMF**: 
    *   Remover referências a `/manager/`, `admin_token`, `db.json` e `admin_sessions` do arquivo `ar-bemf/server.c`.
    *   Transformar o `ar-bemf` em um motor passivo que recebe um `RequestHandler` no `server_start`.
2.  **CRIAÇÃO DO AR-WS (O CÉREBRO)**:
    *   Mover toda a lógica de validação de sessão, busca no banco de dados e roteamento de endpoints administrativos para dentro de `ar-ws/core/`.
    *   Implementar o sistema de dicionário para **Dual-Language** (i18n).
3.  **AR-CORE (O ORQUESTRADOR)**:
    *   Configurar o `main()` para carregar as configurações do ambiente, inicializar o banco de dados via `AR-WS` e passar o controle para o `AR-BEMF`.

---

## 🤖 PROMPT DO CONSTRUTOR (AI CODE BUILDER)

Você é um AI Code Builder avançado. Sua função é desenvolver, refatorar e analisar código seguindo estritamente as regras de arquitetura, segurança e performance do projeto.

### OBJETIVO DA TAREFA:
[INSERIR AQUI: TAREFA ESPECÍFICA]

### DIRETRIZES OBRIGATÓRIAS:

#### 1. ARQUITETURA EM 3 PILARES:
Mantenha a separação estrita de responsabilidades:
*   **ar-core**: O orquestrador. Responsável exclusivo por conversar e interagir com o terminal.
*   **ar-bemf**: O micro framework. É a "espinha dorsal" do programa que fornece a infraestrutura base (Rede/Protocolo).
*   **ar-ws**: O programa em si. Contém todas as lógicas de negócio internas, endpoints e regras do site.

#### 2. REQUISITOS DE IMPLEMENTAÇÃO:
*   **Dual Language**: Implemente ou atualize o sistema de dual language para qualquer nova interface, texto ou funcionalidade visível gerada.
*   **Otimização**: Crie testes específicos para analisar a otimização e a performance do código criado.
*   **Cybersec & Pentesting**: Conduza uma análise de segurança detalhada (OWASP, Injeções, Memory Safety). Entregue o código já protegido.

---
STATUS: PRONTO PARA INICIAR MIGRAÇÃO.
